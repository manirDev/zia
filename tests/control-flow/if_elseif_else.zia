// @author Manir
// @description `si` with `sinon si` and `sinon`
// @tag condition
// @importance 2
var age = 17;

si (age < 13) {
    afficher "enfant\n";
} sinon si (age < 18) {
    afficher "ado\n";
} sinon {
    afficher "adulte\n";
}

// @description `si` with `sinon si` and `sinon`
// @tag condition
// @importance 2
{
    var age = 27;
    si (age < 3) {
        afficher "bébé\n";
    } sinon si (age < 6) {
        afficher "petite enfance\n";
    } sinon si (age < 30) {
        afficher "adulte jeune\n";
    } sinon si (age < 45) {
        afficher "adulte\n";
    } sinon {
        afficher "sénior\n";
    }
}

{
// @description `si` with `sinon si` and `sinon`
// @tag condition
// @importance 2
    var age = 11;
    si (age < 13) {
        afficher "enfant\n";
    } sinon si (age < 18) {
        afficher "ado\n";
    } sinon {
        afficher "adulte\n";
    }
}
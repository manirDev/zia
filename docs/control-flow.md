### Structures conditionnelles

```zia
si (nombre > 0) {
  afficher("Positif");
} sinon {
  afficher("Négatif ou nul");
}
```

### Boucles

```zia
pour (var i = 0; i < 5; i = i + 1) {
  afficher(i);
}
```